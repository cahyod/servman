# Service Manager - Python Dependencies
# Install with: pip install -r requirements.txt
# Or on Ubuntu: sudo apt install python3-gi python3-gi-cairo gir1.2-gtk-3.0

# GTK 3 bindings (system package on Ubuntu/Debian)
# sudo apt install python3-gi python3-gi-cairo gir1.2-gtk-3.0
PyGObject>=3.42.0

# Note: Most dependencies are system packages on Linux
# For Ubuntu/Debian:
#   sudo apt install python3-gi python3-gi-cairo gir1.2-gtk-3.0 polkit
#
# For Fedora:
#   sudo dnf install python3-gobject gtk3 polkit
#
# For Arch Linux:
#   sudo pacman -S python-gobject gtk3 polkit
